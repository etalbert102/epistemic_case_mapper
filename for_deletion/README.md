# Quarantined For Deletion

Nothing in this directory is part of the active contest submission, runtime
imports, or canonical reviewer path. Files were moved here instead of deleted
so a human can inspect or recover them before permanent removal.

| Family | Reason quarantined |
| --- | --- |
| `artifacts/real_briefs/` | Stale generated briefing predating current fail-closed truth-boundary gates |
| `artifacts/comparisons/`, editor, and direct-synthesis runs | Iterative generated outputs superseded by curated examples and evaluation summaries |
| `artifacts/investigator_challenge/raw/` and `inputs/` | Bulk replay payload; representative evidence is curated under `examples/investigator_challenge/supporting/` |
| `artifacts/test_scratch/` | Pytest scratch directories, not repository content |
| `artifacts/cli_probes/` | Diagnostic command output created during verification, not submission evidence |
| `artifacts/reproducibility_gate_20260719/` | Fresh generated outputs retained after the final passing gate instead of being deleted |
| `docs/duplicate_submission/` | Competing narratives superseded by `docs/submission/WRITEUP.md` |
| `docs/duplicate_contest_narratives/` | Before/after and challenge explanations merged into canonical submission and example documents |
| `docs/stale/` and `docs/superseded/` | Broken or outdated documentation removed from active validation |
| `docs/baselines/` | Iterative prototype run dumps; curated baseline documents remain under active `docs/baselines/` |
| `docs/archive/judge/` and `docs/archive/human_audit/` | Older reviewer guides superseded by `docs/START_HERE.md` and `docs/review/` |
| `experiments/` | Placeholder stress-demo package not used by the contest path |
| `examples/merged_indexes/` | Standalone best-region pointers merged into each case README |

Do not cite these files as current evidence. Permanent deletion should happen
only after human confirmation that no debugging or historical record is still
needed.
