# LHC Best Regions

Status: `human-review-needed`

## Strongest Claim Cluster

`worked_region_cosmic_ray_map.md` claims `lhc_c001` through `lhc_c004` show the core natural-exposure argument plus the velocity caveat. This is the fastest judge entry point because it demonstrates why a true map is better than a single reassurance sentence.

## Strongest Relation Cluster

Relations `lhc_r003`, `lhc_r004`, and `lhc_r016` connect the high-level velocity caveat to GM's trapping analysis. This cluster shows decision-space preservation: a caveat is not left as prose but linked to the inference it modifies.

## Strongest Crux

The strongest crux is whether Plaga's metastable/Eddington-limited scenario creates a real gap in GM's compact-star bounds. Inspect `lhc_c013` through `lhc_c016` and relations `lhc_r010` through `lhc_r015`.

## Strongest Preserved Caveat Or Disagreement

The best preserved disagreement is Plaga's white-dwarf stopping objection versus GM's response. The map keeps the scenario distinction, the semiclassical stopping challenge, the 23-order calculation dispute, and the Eddington-limited accretion response separate.

## Strongest Flat-Synthesis Loss

`decision_space_erosion_audit.md` loss `lhc_loss_001` is the clearest erosion example: the flat baseline mentions the velocity caveat but loses the dependency between velocity, trapping, and why compact-star analysis is needed.
