# Frozen-Snapshot Restoration Diff

```diff
--- corrupted_relation

+++ repaired_relation

@@ -1 +1 @@

-- `lhc_r004` (supports): `lhc_c004` -> `lhc_c012`. GM's trapping analysis provides the technical version of the velocity caveat.

+- `lhc_r004` (supports): `lhc_c012` -> `lhc_c004`. GM's trapping analysis provides the technical version of the velocity caveat.

```
