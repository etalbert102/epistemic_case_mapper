# LHC Black Holes Example

Evidence mode: `source_grounded`

Review status: `draft`

Source manifest: `data/cases/lhc_black_holes/case.yaml`

Generation command:

```bash
PYTHONPATH=src python3 scripts/build_case_map.py --case data/cases/lhc_black_holes/case.yaml --output-root examples
```

Validation command:

```bash
PYTHONPATH=src python3 scripts/build_case_map.py --case data/cases/lhc_black_holes/case.yaml
PYTHONPATH=src python3 scripts/validate_case_artifact.py --case data/cases/lhc_black_holes/case.yaml --examples examples/lhc_black_holes
```

## Status

This is a source-grounded draft example. It demonstrates the artifact shape, audit surface, source-local corpus, and regeneration workflow. It still requires claim/relation audit before the review status can be upgraded.

## Known Limitations

- Claims are heuristic candidates and require audit.
- Relations are generated by starter heuristics and should not be treated as settled.
- The text corpus is local, but source-span precision is still coarse.
- Human review has not occurred.
